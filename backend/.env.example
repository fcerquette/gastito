# Server
PORT=3000
NODE_ENV=development

# Database (Supabase Postgres)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres

# JWT
JWT_SECRET=cambia-esto-por-algo-largo-y-aleatorio-min-32-caracteres
JWT_EXPIRES_IN=30d

# Google OAuth (https://console.cloud.google.com/)
GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=tu-client-secret
# En local apunta al backend directo. En producción apunta al frontend
# (https://tu-frontend/api/auth/google/callback) para que el proxy del static
# site reenvíe la callback y la cookie quede first-party. Ver DEPLOY.md.
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# Frontend URL (para CORS y redirect post-login)
FRONTEND_URL=http://localhost:5173

# Cookie domain (dejar vacío en local, en prod poner el dominio)
COOKIE_DOMAIN=
